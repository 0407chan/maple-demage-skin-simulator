import { theme, ThemeConfig } from 'antd'

export const antdTheme: ThemeConfig = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorPrimary: '#f4af38'
  },
  components: {
    Button: {
      colorPrimary: '#f4af38'
    }
  }
}
